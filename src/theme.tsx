import { extendTheme } from '@chakra-ui/core';

export const discordtheme = extendTheme({
  colors: {
    custom: {
      blue: '#7289DA',
      grey: '#99aab5',
      semiBlack: '#2c2f33',
      black: '#23272a',
    },
  },
});
